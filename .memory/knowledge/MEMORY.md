# Knowledge index

Curated facts, one per file, loaded into every session. Keep each entry short —
every line is a permanent per-turn context cost. Delete what turns out to be wrong.

Append-only work history lives separately in `.memory/history/` and is **not**
auto-loaded; read `.memory/history/INDEX.md` on demand.

_No entries yet._
